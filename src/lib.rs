/*!
This crate provides a library for text preprocessing, including tags(HTML, URL, Email) extraction
and removing, punctuation normalization, language extraction. It aims to provide an easy-to-use
text processing toolkit.
*/

#![deny(missing_docs)]

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
